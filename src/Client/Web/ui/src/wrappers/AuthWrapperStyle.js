// material-ui
import { styled } from '@mui/material/styles';

// ==============================|| AUTHENTICATION 1 WRAPPER ||============================== //

const AuthWrapperStyle = styled('div')(({ theme }) => ({
    backgroundColor: "#E3F2F2",
    minHeight: '100vh'
}));

export default AuthWrapperStyle;